//package chapter3;

import java.util.*;

public class TriangleClass {

    /*public static void main(String[] args) {
        double a, b, c, s, area;
        Scanner in = new Scanner(System.in);

        System.out.printf("Enter 3 edges:");
        // 给三角形的三个边赋值
        a = in.nextDouble ();
        b = in.nextDouble ();
        c = in.nextDouble ();

        // 根据给出的公式，计算三角形的面积
        s = (a + b + c)/2;

        area = Math.sqrt (s * (s - a) * (s - b) * (s - c));
        
        // 输出三角形的边 
        System.out.printf("a = %4.2f,  b = %4.2f, c = %4.2f", a, b, c);
        
        // 输出三角形的
        System.out.println ();
        System.out.printf("area = %4.2f", area);
    }*/
}
