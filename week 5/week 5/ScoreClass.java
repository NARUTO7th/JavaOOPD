///**/package chaper4;

/**
 * @author pisces
 *
 *//*
public class ScoreClass {
	
		// 定义成员变量：学号、成绩

		// 定义构造函数
		
		// 设置、获取成员变量的函数	
		
		// 覆盖Object中的成员方法
		public String toString() {
			
		}
		
		// 覆盖Object中的成员方法
	    public boolean equals(Object obj) { 
	    	if (this == obj) {   				// 是否引用同一个对象
	            return true;
	        }
	        if (obj == null) {   				// 是否为空
	            return false;
	        }
	        if (getClass() != obj.getClass()) {	// 是否属于同一个类型
	            return false;
	        }
	        
	        ScoreClass other = (ScoreClass)obj;	  
	        // 当obj为ScoreClass类对象时，比较其内容
	        
		}
	}
}*/
